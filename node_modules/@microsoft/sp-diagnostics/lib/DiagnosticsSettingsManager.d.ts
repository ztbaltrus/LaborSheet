/**
 * @copyright Microsoft Corporation. All rights reserved.
 */
import { Guid } from '@microsoft/sp-core-library';
import IDiagnosticsSettings from './IDiagnosticsSettings';
/**
 * TelemetrySettingsManager stores the configuration for Telemetry passed
 * down by the calling application during bootstrapping.
 *
 * @internal
 */
export default class DiagnosticsSettingsManager {
    static settings: IDiagnosticsSettings;
    protected static readonly enableCircularBufferLogging: Guid;
    private static _initialized;
    static initialize(settings: IDiagnosticsSettings): void;
    static updateSettings(settings: Partial<IDiagnosticsSettings>): void;
    static readonly isInitialized: boolean;
    static shouldUseBuffer(): boolean;
    private static _normalizeId(id);
    private static _normalizeSiteIdWebId();
}
