import { ITraceHandler, ITraceLogEvent, IVerboseTraceLogEvent } from './ITraceHandler';
/**
 * Default implementation for the Trace logger class.
 */
export default class DefaultTraceHandler implements ITraceHandler {
    /**
     * Default implementation for log function.
     */
    log(traceLogEvent: ITraceLogEvent): void;
    /**
     * Default implementation for verbose function.
     */
    verbose(verboseTraceEvent: IVerboseTraceLogEvent): void;
}
