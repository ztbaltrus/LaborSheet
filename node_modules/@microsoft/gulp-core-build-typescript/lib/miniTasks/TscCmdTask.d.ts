/// <reference types="node" />
import { BaseCmdTask, IBaseCmdTaskConfig } from './BaseCmdTask';
/**
 * @public
 */
export interface ITscCmdTaskConfig extends IBaseCmdTaskConfig {
    /**
     * Glob matches for files to be passed through the build.
     */
    staticMatch?: string[];
    /**
     * Removes comments from all generated `.js` files in the TSConfig outDir. Will **not** remove comments from
     * generated `.d.ts` files. Defaults to false.
     */
    removeCommentsFromJavaScript?: boolean;
}
/**
 * @alpha
 */
export declare class TscCmdTask extends BaseCmdTask<ITscCmdTaskConfig> {
    constructor();
    loadSchema(): Object;
    executeTask(gulp: Object, completeCallback: (error?: string) => void): Promise<void> | undefined;
    protected _onData(data: Buffer): void;
    private _removeComments(commandLineArgs);
}
