import { ServiceScope, Guid } from '@microsoft/sp-core-library';
import ISPPageChrome from './ISPPageChrome';
import SPThemeProvider from './SPThemeProvider';
export default class SPPageChrome implements ISPPageChrome {
    static readonly DEFER_LOAD_THEME_KILL_SWITCH: Guid;
    suiteNavDiv: HTMLDivElement;
    appDiv: HTMLDivElement;
    propertyPaneContainerDiv: HTMLDivElement;
    appAndPropertyPaneDiv: HTMLDivElement;
    dialogContainerDiv: HTMLDivElement;
    private _spThemeProvider;
    private _chromeDiv;
    private _isDisposed;
    constructor(serviceScope: ServiceScope);
    changeSuiteNavHeight(pixels: string): void;
    readonly themeProvider: SPThemeProvider;
    /**
     * Shows the page chrome in the current document.
     */
    show(): void;
    /**
     * Hides the page chrome in the current document.
     */
    hide(): void;
    /**
     * Disposes the Chrome
     */
    dispose(): void;
    /**
     * Returns true if the object has already been disposed, otherwise false.
     */
    readonly isDisposed: boolean;
    /**
     * Creates div elements that are used to render the Standard Page Chrome for
     * Client Side Applications. The structure of the Page Chrome is:
     *
     * SPPageChrome - Parent Div for Client Side Applications
     * -SuiteNav - O365 SuiteNav
     * --App - Div Element where the application will be rendered.
     */
    private _createPageChrome();
    private readonly _isFlexLayoutEnabled;
}
