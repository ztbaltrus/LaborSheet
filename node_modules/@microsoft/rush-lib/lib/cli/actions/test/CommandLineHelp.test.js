"use strict";
// Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT license.
// See LICENSE in the project root for license information.
Object.defineProperty(exports, "__esModule", { value: true });
const colors = require("colors");
const RushCommandLineParser_1 = require("../RushCommandLineParser");
describe('CommandLineHelp', () => {
    const parser = new RushCommandLineParser_1.RushCommandLineParser();
    it('prints the global help', () => {
        const helpText = colors.stripColors(parser.renderHelpText());
        expect(helpText).toMatchSnapshot();
    });
    for (const action of parser.actions) {
        it(`prints the help for "rush ${action.actionName}"`, () => {
            const helpText = colors.stripColors(action.renderHelpText());
            expect(helpText).toMatchSnapshot();
        });
    }
});

//# sourceMappingURL=CommandLineHelp.test.js.map
