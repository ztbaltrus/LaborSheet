import { IPackageDeps } from '@microsoft/package-deps-hash';
import { RushConfiguration } from '../../data/RushConfiguration';
export declare class PackageChangeAnalyzer {
    static getPackageDeps: (path: string, ignoredFiles: string[]) => IPackageDeps;
    private _data;
    private _rushConfiguration;
    private _isGitSupported;
    constructor(rushConfiguration: RushConfiguration);
    getPackageDepsHash(projectName: string): IPackageDeps | undefined;
    private _getData();
    private _getProjectForFile(filepath);
    private _fileExistsInFolder(filePath, folderPath);
}
